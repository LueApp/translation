# Cloudflare deploy for the AI Translate landing page.
# Serves the static `site/` directory (page + downloads) as Worker assets.
#   npx wrangler deploy
name = "translation"
compatibility_date = "2026-05-01"

[assets]
directory = "./site"
